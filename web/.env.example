# Dev-only: where `npm run dev` proxies /api requests to.
# In production the built app is served same-origin by the Discord Recall
# backend, so relative /api paths need no configuration at all.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
